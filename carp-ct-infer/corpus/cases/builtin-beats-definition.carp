;; expect: accept
;; why: the evaluator tries a builtin before it looks a name up, so the bare
;; `length` here is the builtin and not the module's own, whatever arity that
;; one has
(defmodule Counter
  (defndynamic length [t] (length (car t)))

  (defndynamic size [t] (Counter.length t)))
