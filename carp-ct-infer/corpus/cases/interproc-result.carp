;; expect: reject
;; why: `render` answers with a String, and `car` takes that apart. The shape
;; crosses a call boundary in the other direction: the result, not the domain
(defndynamic render [form] (str form))

(defndynamic head-of-rendered [form] (car (render form)))
