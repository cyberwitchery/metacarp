;; expect: accept
;; why: the binding shadows the parameter, so the use below it says nothing about
;; what the caller passed
(defndynamic head-of-own-list [x] (let [x (list 1 2)] (car x)))

(defndynamic use-it [] (head-of-own-list 3))
