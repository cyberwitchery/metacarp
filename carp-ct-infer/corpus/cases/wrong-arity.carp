;; expect: reject
;; why: a call into a compile-time definition with the wrong number of arguments
;; never reaches a body, and the definition is not a builtin that could be
;; variadic
(defmodule Pair (defndynamic make [a b] (list a b)))

(defndynamic one-sided [x] (Pair.make x))
