;; expect: accept
;; why: a macro's parameters bind unevaluated syntax, so the argument at the call
;; site is a form and not the value the form would produce
(defmacro first-of [form] (car form))

(defmacro use-it [] (first-of (list 1 2)))
