;; expect: accept
;; why: `macro-error` has shape ⊥, and unreachable code contradicts nothing, so
;; a correctly guarded raise is not a finding
(defmacro require-list [x]
  (if (list? x) (car x) (macro-error "require-list needs a list")))
