;; expect: accept
;; why: inside a quasiquote a form is data. The `car` here is syntax being built,
;; not an operation this body performs
(defmacro emit-a-car [x] (quasiquote (car (quote (unquote x)))))
