;; expect: accept
;; why: the sequence operation is inside the branch the guard protects, so a
;; caller passing an Int never reaches it. A domain read off a branch rather than
;; off the spine would reject this
(defndynamic maybe-head [x] (if (list? x) (car x) x))

(defndynamic use-it [] (maybe-head 3))
