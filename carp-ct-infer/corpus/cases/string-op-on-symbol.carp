;; expect: reject
;; why: a quoted symbol is a Symbol, and no String operation accepts one, though
;; the compile-time language otherwise treats a symbol and its spelling alike
(defndynamic describe-binding [name] (String.append "binding " (quote missing)))
