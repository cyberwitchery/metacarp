; two libraries define globals and helpers under the same names; each must
; see its own when both are loaded into one host.
(def greeting @"alpha")
(private label)
(defn label [] &greeting)
(defn alpha-greet [] (IO.println (label)))
