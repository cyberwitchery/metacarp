(Project.no-echo)
(defn classify [n] (cond (< n 0) @"neg" (= n 0) @"zero" @"pos"))
(defn main []
  (do
    (when true (println* &(classify -1)))
    (unless false (println* &(classify 0)))
    (for [i 1 3] (println* &(classify i)))))
